server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test?currentSchema=cuentas
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    schemas: cuentas
    default-schema: cuentas
  rabbitmq:
    host: localhost
    port: 5672

springdoc:
  api-docs:
    path: /api-docs
